# MCP Skill Install

Install the local skill by copying `skill/skill.md` and `skill/handler.py` into the client skill directory, then configure the client to call the local `job_ftch mcp-server`.

Recommended trigger commands:

- `/job-search`
- `/jobs`
- `/run-jobs`
