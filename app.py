"""job_ftch — entry point. Composition root: assembles pipeline from config."""

# This file will wire together: Source + [Node...] + Sink + Store
# and run the async pipeline loop.
# Implementation comes in the next milestone.

if __name__ == "__main__":
    print("job_ftch pipeline — not yet implemented. See docs/architecture.md")
