# Инструменты документации. Версии закреплены, чтобы раннер и локальный запуск
# давали одинаковый результат. Ставится через just setup.

mkdocs==1.6.1
mkdocs-material==9.7.7
pymdown-extensions==11.0.1

# Security pins for transitive dependencies used by the documentation plugins.
GitPython==3.1.58
idna==3.18

# Навигация из структуры папок вместо ручного nav.
mkdocs-awesome-pages-plugin==2.10.1

# Дата последнего изменения из git рядом с last_verified.
mkdocs-git-revision-date-localized-plugin==1.3.0

# Разбор frontmatter в линтерах и проверка стиля YAML.
PyYAML==6.0.2
yamllint==1.35.1
