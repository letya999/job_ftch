FROM python:3.12-slim

WORKDIR /app

COPY . /app

RUN pip install --no-cache-dir .

CMD ["job_ftch", "mcp-server", "--configs-dir", "/app/config", "--transport", "http", "--host", "0.0.0.0", "--port", "8000"]
