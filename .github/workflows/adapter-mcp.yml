name: adapter-mcp

on:
  push:
    paths:
      - "job_ftch/adapters/mcp/**"
      - "job_ftch/**"
      - "pyproject.toml"
  pull_request:
    paths:
      - "job_ftch/adapters/mcp/**"
      - "job_ftch/**"

jobs:
  build:
    name: Lint & build image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Python + uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
      - name: Lint adapter
        run: uvx ruff check job_ftch/adapters/mcp
      - name: Build image
        run: docker build -f job_ftch/adapters/mcp/Dockerfile -t job-ftch-mcp .
