export function GET() { return new Response(`# job_ftch — machine notes\n\n## Product\nAsync pipeline that collects Telegram, career-site, RSS and API vacancy signals, normalizes them into structured records, deduplicates them and publishes accepted jobs.\n\n## API\nGET /api/v1/sources returns public source inventory. GET /api/v1/jobs?limit=20 returns canonical vacancy cards from PostgreSQL. Responses are JSON, read-only, cached for 15 seconds and limited to 60 requests per minute per IP.\n\n## Safety\nUse fair use, cache responses, respect source terms and do not automate applications without a human in the loop.\n`, { headers: { "Content-Type": "text/plain; charset=utf-8", "Cache-Control": "public, max-age=3600" } }); }
