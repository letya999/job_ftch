# job_ftch MCP Skill

Use this skill when the user wants to run job ingestion, inspect tenant status, or search vacancies through the `job_ftch` MCP server.

Triggers:
- `/job-search`
- `/jobs`
- `/run-jobs`

The handler maps skill invocations to the MCP tools exposed by `job_ftch mcp-server`.
