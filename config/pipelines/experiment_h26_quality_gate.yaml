extends: experiment_h11_no_late_scoring.yaml
name: experiment_h26_quality_gate
version: "1.0.0"
metadata:
  purpose: isolate_quality_as_defensive_gate
  experiment: {id: H26-quality, control: H11}
  benchmark: {dataset: fixtures/dataset/eval_dataset.jsonl, sample: 140, seed: 42}
nodes:
  - {id: completeness, node: completeness_gate, effect: observe, after: [semantic_prefilter]}
  - {id: lifecycle, node: lifecycle, effect: observe, after: [compensation]}
  - {id: is_job, node: is_job, effect: observe, after: [lifecycle]}
  - {id: risk, node: risk, effect: observe, after: [is_job]}
  - {id: quality, node: quality, effect: gate, after: [risk]}
  - {id: llm_relevance, node: llm_relevance, after: [quality]}
