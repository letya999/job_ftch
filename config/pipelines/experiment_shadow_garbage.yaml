extends: historical_best.yaml
name: experiment_shadow_garbage
version: "1.0.0"
metadata:
  purpose: authority_ablation
  experiment: {variable: node:garbage_filter.authority}
nodes:
  - {id: garbage_filter, authority: shadow}
